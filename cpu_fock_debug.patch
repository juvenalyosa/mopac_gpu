diff --git a/src/SCF/fock2.F90 b/src/SCF/fock2.F90
index 0000000..1111111 100644
--- a/src/SCF/fock2.F90
+++ b/src/SCF/fock2.F90
@@
-      double precision :: sumdia, sumoff, sum, aa, bb, aj, ak, a
-      logical :: lid, deriv
+      double precision :: sumdia, sumoff, sum, aa, bb, aj, ak, a
+      logical :: lid, deriv
+      logical :: dump_fock_map
+      integer :: dump_unit, dump_env_status
+      character(len=16) :: dump_line
@@
-      if (numat == 0) then
+      dump_fock_map = .false.
+      dump_unit = -1
+      dump_env_status = 1
+      dump_line = ''
+      if (numat == 0) then
           if (allocated(ptot2))  deallocate(ptot2)
           if (allocated(ifact))  deallocate(ifact)
           if (allocated(i1fact)) deallocate(i1fact)
            return
         end if
@@
           lid = id == 0
           ione = 1
           if (id /= 0) ione = 0
+          call get_environment_variable('MOPAC_DEBUG_FOCK_INDEX', dump_line, status=dump_env_status)
+          if (dump_env_status == 0) then
+            if (trim(adjustl(dump_line)) /= '') then
+              dump_fock_map = .true.
+              dump_unit = 97
+              open(unit=dump_unit, file='gpu_fock_cpu_map.csv', status='replace', action='write')
+              write(dump_unit,'(A)') 'pair_kind,ii,jj,i,j,k,l,pos'
+            end if
+          end if
@@
                   if (i > 0) then
                     do j = 1, i
                       f(j+j1) = f(j+j1) + ptot(ll)*w(j+kk+k)
                       sumoff = sumoff + ptot(j+j1)*w(j+kk+k)
+                      if (dump_fock_map) then
+                        write(dump_unit,'("HL_C",",",I0,",",I0,",",I0,",",I0,",",I0,",",I0,",",I0)') &
+     &                    ii, jj, ia+i, ia+j-1, ja, ja, j+kk+k
+                      end if
                     end do
                     k = i + k
                     j1 = i + j1
                   end if
                   j1 = j1 + 1
                   k = k + 1
                   f(j1) = f(j1) + ptot(ll)*w(kk+k)
                   sumdia = sumdia + ptot(j1)*w(kk+k)
+                  if (dump_fock_map) then
+                    write(dump_unit,'("HL_C",",",I0,",",I0,",",I0,",",I0,",",I0,",",I0,",",I0)') &
+     &                ii, jj, ia+i, ia+i, ja, ja, kk+k
+                  end if
                 end do
@@
                   do j = 1, ib - ia + 1
                     sum = sum + p(ifact(j-1+ia)+ja)*w(kk+jindex(j+k))
+                    if (dump_fock_map) then
+                      write(dump_unit,'("HL_X",",",I0,",",I0,",",I0,",",I0,",",I0,",",I0,",",I0)') &
+     &                  ii, jj, i, ia+j-1, ja, ia+j-1, kk+jindex(j+k)
+                    end if
                   end do
@@
                   if (i > 0) then
                     do j = 1, i
                       f(j+j1) = f(j+j1) + ptot(ll)*w(j+kk+k)
                       sumoff = sumoff + ptot(j+j1)*w(j+kk+k)
+                      if (dump_fock_map) then
+                        write(dump_unit,'("HLR_C",",",I0,",",I0,",",I0,",",I0,",",I0,",",I0,",",I0)') &
+     &                    ii, jj, ja+i, ja+j-1, ia, ia, j+kk+k
+                      end if
                     end do
                     k = i + k
                     j1 = i + j1
                   end if
                   j1 = j1 + 1
                   k = k + 1
                   f(j1) = f(j1) + ptot(ll)*w(kk+k)
                   sumdia = sumdia + ptot(j1)*w(kk+k)
+                  if (dump_fock_map) then
+                    write(dump_unit,'("HLR_C",",",I0,",",I0,",",I0,",",I0,",",I0,",",I0,",",I0)') &
+     &                ii, jj, ja+i, ja+i, ia, ia, kk+k
+                  end if
                 end do
@@
                   do l = 1, 4
                     sum = sum + p(l-1+k)*w(kk+jindex(l+j))
+                    if (dump_fock_map) then
+                      write(dump_unit,'("HLR_X",",",I0,",",I0,",",I0,",",I0,",",I0,",",I0,",",I0)') &
+     &                  ii, jj, ia, ja+l-1, ia, ja+l-1, kk+jindex(l+j)
+                    end if
                   end do
@@
                 f(j1) = f(j1) + ptot(i1)*w(kk+1)
                 f(ij) = f(ij) - p(ij)*w(kk+1)
+                if (dump_fock_map) then
+                  write(dump_unit,'("LL",",",I0,",",I0,",",I0,",",I0,",",I0,",",I0,",",I0)') &
+     &              ii, jj, ia, ja, ia, ja, kk+1
+                end if
                 kk = kk + 1
               end if
@@
         if (useps) then
           call addfck (f,ptot)
         end if
+        if (dump_fock_map .and. dump_unit > 0) then
+          close(dump_unit)
+        end if
         return
